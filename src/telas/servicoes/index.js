import { Text, SafeAreaView, StatusBar } from 'react-native';

export default function Servicos() {
    return (
        <SafeAreaView>
            <StatusBar/>
            <Text> Gatito!!! </Text>
        </SafeAreaView>
    )
}